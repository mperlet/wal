\documentclass[11pt,a4paper,sans][rundauf]moderncv[rundzu]       
\moderncvstyle[rundauf]banking[rundzu]                     
\moderncvcolor[rundauf]red[rundzu]                                
\usepackage[utf8][rundauf]inputenc[rundzu]                       % if you are not using xelatex ou lualatex, replace by the encoding you are using
\usepackage[scale=0.75][rundauf]geometry[rundzu]
\name[rundauf]{{ absname }}[rundzu]
\title[rundauf][rundzu]                               % optional, remove / comment the line if not wanted
\address[rundauf]{{ absstreet }}[rundzu][rundauf]{{ abslocationnum }}-{{ abslocation }}[rundzu]% optional, remove / comment the line if not wanted; the "postcode city" and and "country" arguments can be omitted or provided empty
\phone[mobile][rundauf]{{ abstelephone }}[rundzu]                   % optional, remove / comment the line if not wanted                    % optional, remove / comment the line if not wanted
\email[rundauf]{{ absemail }}[rundzu]                               % optional, remove / comment the line if not wanted
\begin[rundauf]document[rundzu]
\recipient[rundauf]{{ empname }}[rundzu][rundauf]{{ empstreet }}\\{{ emplocation }} {{ emplocationnum }}[rundzu]
\date[rundauf]\today[rundzu]
\opening[rundauf]{{ title }},[rundzu]
\closing[rundauf]{{ greeting }}[rundzu]
\makelettertitle

{{ text }}
\makeletterclosing

\end[rundauf]document[rundzu]


